{
  "name": "@ai-crew-suite/scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "lint": "ai-crew-eslint --role node src --max-warnings 0",
    "run:sync-refs": "tsx src/sync-project-references.ts",
    "test:unit": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.23.13"
  },
  "devDependencies": {
    "@backstage/cli": "backstage:^",
    "@types/node": "catalog:node-types",
    "rimraf": "6.1.3",
    "typescript": "catalog:typescript"
  }
}
